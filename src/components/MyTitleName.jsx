export default function MyTitleName({title}){
    return (
        <div className="text-center p-2">
            <span className="border-b p-2 border-red-500 font-bold text-xl">{title}</span>
        </div>
    );
}